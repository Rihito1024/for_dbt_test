name: Example Workflow
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Use secret in script
        env:
          MY_SECRET: ${{ secrets.DBT_PASSWORD }}
        run: |
          echo "Secret is $MY_SECRET"
          # 実際にはechoしないでください。例示のみ
